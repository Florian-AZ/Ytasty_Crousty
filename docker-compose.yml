services:
  db:
    image: postgres:16
    container_name: ytasty-db
    environment:
      POSTGRES_USER: ytasty
      POSTGRES_PASSWORD: ytasty_password
      POSTGRES_DB: ytasty
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: